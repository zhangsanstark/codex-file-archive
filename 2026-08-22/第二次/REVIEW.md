# 单词助记生产迭代 01：10 词人工反馈

生成日期：2026-08-22  
生产配置：GLM 5.3；每词 1 次请求；每词最多 2 案；无重试  
上下文：4 条 V4.2 结构化 few-shot + `douyin-word-memory-100-v1` 全部 10 条方法路线  
执行结果：10 次请求，9 个合同有效、1 个合同无效，共 77,791 tokens  
确定性生产门禁：`PASS 4 / REWORK 5 / REJECT 1`  
三路人工复核的共同结论：`KEEP 1 / 未达到直接生产标准 9`。不同审阅者对其中几案应算 `REWORK` 还是 `REJECT` 有分歧，但都不同意直接放行。机器的 `PASS` 仍有 3 个内容假阳性；`auxiliary` 的机器 `REJECT` 则包含全角加号造成的格式假阴性。

说明：`PASS` 只表示结构上能拼回、字段和错拼锁齐全，不等于真的好记。下面保留全部 10 个原始方案，正是为了收集你的品味反馈。你可以直接在每项末尾填写，也可以在对话里回复“编号 + 保留/修改/淘汰 + 原因”。

## 快速总览

| # | 单词 | 机器门禁 | 我的初判 |
|---:|---|---|---|
| 1 | personnel | PASS | REWORK |
| 2 | psychology | REWORK | REJECT / 重做 |
| 3 | handkerchief | REWORK | REWORK |
| 4 | calendar | PASS | REWORK |
| 5 | acquaintance | PASS | REWORK |
| 6 | successful | PASS | KEEP 候选 |
| 7 | sovereign | REWORK | REJECT / 重做 |
| 8 | cemetery | REWORK | 可讨论，偏 REWORK |
| 9 | auxiliary | REJECT | 可讨论，修格式后 REWORK |
| 10 | refrigerator | REWORK | REJECT / 重做 |

## 1. PERSONNEL — 人员；人事部门

- 分块：`PERSON + NEL`
- 记法：人事部门先认下“人”，再进“内里”；“内”完整写出，“里”只取首字母；人与内之间两个 n，词尾只有一个 l。
- 回忆链：人员 → 认人加内里 → `PERSON + NEL` → `PERSONNEL`
- 模型自报风险：“内里的里只取首字母”需要额外训练，可能误写成 `NEILI`。
- 机器门禁：`PASS`
- 我的意见：`REWORK`。“内”的完整拼音是 `NEI`，再加“里”的首字母会得到 `NEIL`，并不能自然得到 `NEL`；这是表面有证据、实际映射不成立的典型。

你的评分（1–5）：  
保留 / 修改 / 淘汰：  
意见：

## 2. PSYCHOLOGY — 心理学；心理

- 分块：`PSYCH + O + LOGY`
- 记法：先完整写出“希腊灵魂词根”，保留开头和中间两个静音字母，再接一个圆圈字母和“学问”后缀。
- 回忆链：心理学 → 灵魂词根、圆圈、学问后缀 → `PSYCH + O + LOGY` → `PSYCHOLOGY`
- 模型自报风险：如果没见过 `PSYCH`，第一块仍需先背。
- 机器门禁：`REWORK`，真实词源需要事实复核。
- 我的意见：`REJECT / 重做`。目标最难处恰好是 `PSYCH`，方案只是要求“完整写出”它，并没有降低负担；静音 p、h 的提醒是纠错规则，不是足够的助记。

你的评分（1–5）：  
保留 / 修改 / 淘汰：  
意见：

## 3. HANDKERCHIEF — 手帕

- 分块：`HAND + KER + CHIEF`
- 记法：手帕是“手里的盖头巾”；保留熟词“手”，中间接固定前段，最后用熟词“首领”锁住 `i-e` 顺序。
- 回忆链：手帕 → 手里的盖头巾 → `HAND + KER + CHIEF` → `HANDKERCHIEF`
- 模型自报风险：`KER` 仍需单独记忆。
- 机器门禁：`REWORK`，词源需要事实复核。
- 我的意见：`REWORK`。`HAND` 和 `CHIEF` 很有用，能锁住 d 与 `i-e`；但 `KER` 没有真正机关，而且把它称为“固定前段”只是换个名字继续背。

你的评分（1–5）：  
保留 / 修改 / 淘汰：  
意见：

## 4. CALENDAR — 日历；日程表

- 分块：`CA + LEND + AR`
- 记法：日历把日子“借出去”：先“擦”掉昨天，再 `LEND` 出今天，最后“啊”一声翻到年底。
- 回忆链：日历 → 擦掉、借出、啊一声 → `CA + LEND + AR` → `CALENDAR`
- 模型自报风险：“啊”主要锁 a，末尾 r 仍依赖语感。
- 机器门禁：`PASS`
- 我的意见：`REWORK`。画面有一点连贯性，`LEND` 也确实锁住中间 e；但“擦”到 `CA`、“啊”到 `AR` 都不完整，尤其 r 没来源。可以作为风格方向，但还不是成品。

你的评分（1–5）：  
保留 / 修改 / 淘汰：  
意见：

## 5. ACQUAINTANCE — 熟人；相识

- 分块：`AC + QUAINT + ANCE`
- 记法：熟人是装空调的古怪人：空调缩写在开头，`QUAINT`（古怪）居中，词尾使用“距离”同款尾巴。
- 回忆链：熟人 → 空调、古怪、距离尾 → `AC + QUAINT + ANCE` → `ACQUAINTANCE`
- 模型自报风险：不认识 `QUAINT` 时，中块会成为新负担。
- 机器门禁：`PASS`
- 我的意见：`REWORK`。如果学习者已经会 `quaint`，它很简洁；否则只是用另一个生词代替目标词。应提供不依赖 `quaint` 的备选方案。

你的评分（1–5）：  
保留 / 修改 / 淘汰：  
意见：

## 6. SUCCESSFUL — 成功的

- 分块：`SUCCESS + FUL`
- 记法：成功的原形在前，“充满”的后缀在后；原形内部两对成对字母都保留，词尾后缀只有一个 l。
- 回忆链：成功的 → 成功原形加后缀 → `SUCCESS + FUL` → `SUCCESSFUL`
- 模型自报风险：如果 `SUCCESS` 内部的两对双写仍不熟，“成对”提示可能不够具体。
- 机器门禁：`PASS`
- 我的意见：`KEEP 候选`。这是本批最简洁的一案：词义能自然触发 `SUCCESS`，再加高频后缀；但仍应把 `cc / ss / 单 l` 的位置做成更鲜明的统一机关。

你的评分（1–5）：  
保留 / 修改 / 淘汰：  
意见：

## 7. SOVEREIGN — 君主；有主权的

- 分块：`SO + VE + REIGN`
- 记法：“之所以”提示开头，胜利手势旁跟一只鹅提示中段，最后完整写出“统治”。
- 回忆链：君主 → 之所以、胜利的鹅、统治 → `SO + VE + REIGN` → `SOVEREIGN`
- 模型自报风险：如果不熟 `REIGN`，第三块只是转移负担。
- 机器门禁：`REWORK`，困难生词依赖。
- 我的意见：`REJECT / 重做`。`VE` 的“胜利的鹅”牵强，`REIGN` 本身也容易拼错；故事没有真正解决核心的 `ereign` 顺序。

你的评分（1–5）：  
保留 / 修改 / 淘汰：  
意见：

## 8. CEMETERY — 墓地；公墓

- 分块：`CE + ME + TERY`
- 记法：“测、么、特瑞”依次对应三块；三个声音各带一处 e，词尾是“特瑞”而不是“他瑞”。
- 回忆链：墓地 → 测么特瑞 → `CE + ME + TERY` → `CEMETERY`
- 模型自报风险：“特瑞”是弱近音，需要先练一次。
- 机器门禁：`REWORK`，前两块共用同一泛化证据。
- 我的意见：`可讨论，偏 REWORK`。它确实覆盖全部字母和三个 e，比本批很多方案完整；但“测么特瑞”与“墓地”的语义联系很弱，像为拼写临时造出的口令。是否好用很依赖你的个人品味。

你的评分（1–5）：  
保留 / 修改 / 淘汰：  
意见：

## 9. AUXILIARY — 辅助的；辅助人员或设备

- 分块：`AUX + ILI + ARY`
- 记法：辅助插口拉出“一里”长线，末端接“字典同款词尾”。
- 回忆链：辅助的 → 插口、一里、字典尾 → `AUX + ILI + ARY` → `AUXILIARY`
- 生成合同错误：回忆链使用了全角加号，未通过严格机械检查。
- 机器门禁：`REJECT`
- 我的意见：`可讨论，修格式后 REWORK`。这里存在两种合理看法：如果学习者本来就认识设备上的 `AUX` 标签，又熟悉 `dictionary` 的 `-ARY` 词尾，那么 `AUX + 一里 + ARY` 可能相当省力；如果不知道这两个锚点，就会变成循环提示和负担转移。因此不应因一个全角加号直接淘汰，但也不能自动通过。

你的评分（1–5）：  
保留 / 修改 / 淘汰：  
意见：

## 10. REFRIGERATOR — 冰箱

- 分块：`RE + FRIG + ER + ATOR`
- 记法：冰箱把食物“再冷一档”：先写“再”，再写冷词的一段，中间接“而”，最后接机器尾巴。
- 回忆链：冰箱 → 再冷一档的机器 → `RE + FRIG + ER + ATOR` → `REFRIGERATOR`
- 模型自报风险：`FRIG` 依赖先认识另一个冷词。
- 机器门禁：`REWORK`，四块超生产上限且含困难生词依赖。
- 我的意见：`REJECT / 重做`。四块负担高，`FRIG` 与 `ATOR` 都不够熟，故事语言也不自然；虽然能拼回，但明显不比直接记忆更省力。

你的评分（1–5）：  
保留 / 修改 / 淘汰：  
意见：

## 请优先告诉我这三件事

1. 第 6 个 `successful` 是否接近你想要的“可直接生产”水平？
2. 第 8 个 `cemetery` 这种完整但略生硬的中文口令，你是喜欢、能接受，还是坚决不要？
3. 你更偏好“熟词/真实构词的简洁方案”，还是可以接受“中文谐音故事”，只要它足够有画面？

下一轮会根据你的回答改生产方式，而不是只改几个词：调整 few-shot 构成、允许的方法族、困难块规则和每词候选数量，再生产新的 10 词。
