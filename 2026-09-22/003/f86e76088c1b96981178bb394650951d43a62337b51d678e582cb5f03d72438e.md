这版口径明确了。**先更新草稿，暂不制作 PPT。** 第 1、2、3、6 页内容保留，重点调整第 4、5、8 页，第 7 页重新配色。

**整体视觉**

采用白底，文字和图形只使用**深红色与黑色**：

- 深红色：章节标题、关键模块、重点数字和风险处置。
- 黑色：正文、说明和普通连接线。
- 流程图通过实线、虚线、边框及适当加粗区分层次，不再使用蓝、绿、金色。
- 截图保留原始内容，重点位置用深红色框标注。

**第 4 页｜输入侧：Skill 版权意图识别**

这一页重点介绍 **Skill 包含什么规则、如何影响回答，以及不同系统的策略建设工作**。

开头用一句话解释：

> **通过 Skill 将版权管控规则交给模型，让模型结合用户请求与会话上下文，判断哪些内容可以回答、哪些需要限制，以及如何提供替代帮助。**

规则内容分成三个部分：

| 规则组成展示内容 |                        |
| -------- | ---------------------- |
| **管控范围** | 哪些作品纳入管控，适用哪些限制        |
| **请求判断** | 原文索取、歌词索取、续写、分批提取及规避请求 |
| **回答方式** | 限制受限内容，提供摘要、分析等替代帮助    |

地域部分明确画成**两个独立系统**，不设置“根据地域选择 Skill”的程序分支：

- **国内系统：**独立建设和部署 Skill，结合风险清单与友商实践，对管控范围内的作品执行相应规则。
- **海外系统：**独立建设和部署 Skill，结合所在市场的法律要求、作品权利及授权情况，制定相应规则。

这样体现的是**不同地域分别开展规则建设与验证**，而不是一个系统中的地域路由。

版式建议：

- **左侧约 45%：**Skill 定义、规则组成，以及国内和海外两套独立策略。
- **右侧约 55%：**同一请求使用 Skill 前后的两张对比截图，突出回答变化。
- 友商截图选取关键回复片段，放在对应策略旁边，避免整屏截图占据过多空间。

**第 5 页｜输出侧：Answer 版权审核与扩容**

这一页补充“能识别什么、满足什么条件会拦截”，形成完整的四层内容。

**上部：审核流程**

> 模型回答 → 文本规范化 → 书籍 / 歌词版权库匹配 → 连续匹配确认 → 放行 / 拦截

**中部左侧：识别能力与匹配规则**

识别能力文案：

> **识别回答中对书籍、歌词的连续原文复述，并支持在配置的容错范围内识别近似匹配。**

具体规则：

| 内容类型拦截条件容错能力 |                               |              |
| ------------ | ----------------------------- | ------------ |
| **书籍**       | 连续匹配片段达到 **100 字**，且满足匹配条件时拦截 | 可配置允许的文字差异比例 |
| **歌词**       | 连续匹配片段达到设定字数时拦截，具体阈值待补充       | 可配置容错参数      |

补充两点说明：

- 对格式、标点等变化进行规范化处理，对少量文字差异按容错配置判断。
- 命中后返回作品、匹配位置和长度，支持复核。

这里的“100 字”指**回答与版权库原文的连续匹配片段**，不是仅根据用户请求中的字数判断。

**中部右侧：覆盖范围与测试结果**

保留书籍数量、歌曲数量、检出率、误检率和审核时延，结合你提供的测试报告展示。

**下部：扩容计划**

> **近期新增 1,200 本书籍，配套调整版权库与检索架构，控制扩容后的审核时延。**

再列两项后续方向：随风险清单扩大覆盖范围，根据业务需求增加其他语言。

**第 7 页｜图片版权与公众人物审核架构**

沿用之前确认的架构，只调整为深红色和黑色。

关键词审核、IP 图片审核、人脸匹配及结果处置的结构保持一致，仍然保留关键词应急接入入口。

**第 8 页｜图片 IP 审核：覆盖扩展与策略优化**

这一页的核心叙事调整为：

> **扩大管控范围后，逐类提升图片识别能力，减少对关键词拦截的依赖，最终将整体端到端误检率控制在 1% 以下。**

覆盖扩展和方案探索保留：

- 整体管控范围从二三十个扩展至 **200 多个分类**，由图片审核和关键词共同覆盖。
- 简述 YOLO World + DINOv3、VLM 等实验，说明综合资源等约束后采用 YOLO 检测方案。
- 目前 **50 个分类**的图片检出率超过 **75%**，已移除对应分类的关键词拦截。

测试结果分为两组：

| 测试范围关键词误检率图片审核误检率端到端误检率 |           |           |           |
| ----------------------- | --------- | --------- | --------- |
| 普通样本                    | —         | **0.31%** | **0.82%** |
| 包含攻击样本                  | **1.96%** | **0.27%** | **超过 1%** |

历史 **4.93%** 单独作为背景说明：

> 历史图片审核覆盖范围较小，测试集也不同，不与当前结果直接比较。

因此不做“4.93% 降至 0.31%”的对比图，也不计算降幅。

下方用一条优化路径突出后续工作：

> **逐类提升图片检出率 → 超过 75% 后移除对应关键词 → 验证端到端效果 → 整体端到端误检率低于 1%**

旁边补一句：

> 当前包含攻击样本时，关键词误检较高，使端到端误检超过 1%。后续通过图片审核逐步替代对应分类的关键词拦截，降低整体误检；关键词继续承担新增高风险 IP 的应急管控。

这样能明确区分：**75% 是逐分类的图片检出门槛，1% 是整体端到端误检目标。**

现在第五页还差一个具体参数：**歌词连续匹配多少字时拦截？** 容错比例如果暂时不需要展示具体数值，就统一写“可配置”。
